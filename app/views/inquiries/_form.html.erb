<%= form_for(@inquiry) do |f| %>
  <% if @inquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inquiry.errors.count, "error") %> prohibited this inquiry from being saved:</h2>

      <ul>
      <% @inquiry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inquiry %><br />
    <%= f.text_field :inquiry %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.number_field :user %>
  </div>
  <div class="field">
    <%= f.label :actiontaken %><br />
    <%= f.check_box :actiontaken %>
  </div>
  <div class="field">
    <%= f.label :question %><br />
    <%= f.number_field :question %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
