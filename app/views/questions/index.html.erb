<!--<h1>Listing questions</h1>-->

	<div class = "span-24 last box">
       		<%=render 'layouts/menu' %>
	</div><!--End span-24 box.-->
	
	<div class = "span-24 last box">
		<%= form_for @search do |f|%>
			<p>
				<%= f.label :question_contains %>
				<%= f.text_field :question_contains %>
			    <class = "button"><%=f.submit "Search iHardie"%>
				<%= sort_link @search, :question%>
				<%= sort_link @search, :priority%>|
			</p>
		<%end%>
	</div>
	
	<div class = "span-24 last box">
	<% if params[:search].blank? %>
		<%= "No matching Answers found yet."%>
	<% else %>
	<table>
	  <tr>
	    <th>Question</th>
	    <th>Answer</th>
	    <!--<th>Product</th>
	    <th>Priority</th>
	    <th>User</th>
	    <th>QuestionType</th>
	    <th>Favorite</th>-->
	  </tr>
	<% @questions.each do |question| %>
	 <tr class= "<%= cycle("rowA", "rowB")%>">
	    <td><%= link_to question.question, question %></td>
	    <td><%= question.answer %></td>
	    <!--<td><%= question.product %></td>
	    <td><%= question.priority %></td>
	    <td><%= question.user %></td>
	    <td><%= question.questiontype %></td>
	    <td><%= question.favorite %></td>
	    <td><%= question.accellink %></td>
	    <td><%= question.hardielink %></td>
	    <td><%= question.youtubelink %></td>-->
		<%if can? :update, question%>
		    <td><%= link_to 'Show', question %></td>
		    <td><%= link_to 'Edit', edit_question_path(question) %></td>
		<%end%>
    			
		<%if can? :destroy, question%>
		    <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<%end%>
	  </tr>
	 <% end %>
	 <%end%>
	</table>
	<br />
</div><!--End span-24 box.-->

<!--<%= link_to 'New Question', new_question_path %>-->

